# 3.2
# Jesli chcemy w L uzyskac posortowana tablice powinno sie uzyc samego L.sort(). Aktualnie L = null
# L = [3, 5, 4] ; L = L.sort()

# Nie mozna w ten sposob przypisac 3 wartosci 2 zmiennym
# x, y = 1, 2, 3

# X jest krotka, wiec zmiana jej wartosci przez podstawienie jest niemozliwe
# X = 1, 2, 3 ; X[1] = 4

# Nie odniesiemy sie do indeksu 3 poniewaz w tabeli sa indeksy 0, 1, 2
# X = [1, 2, 3] ; X[3] = 4

# X jest stringiem, wiec nie ma metody append()
# X = "abc" ; X.append("d")

# Funkcja pow oczekuje 2 argumentow
# L = list(map(pow, range(8)))
